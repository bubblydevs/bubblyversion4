const { MessageEmbed } = require('discord.js')
const fetch = require("node-fetch");

module.exports = {
  name: "yhyhpipedown",
  run: async (client, message, args) => {
    
  },
  ownerOnly: true,
};
